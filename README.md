# ShopOS Releases

Update channel for **ShopOS Theme** and **ShopOS Core**.

- `manifest.json` — polled by installed sites; points at the latest packages.
- Zips are attached to GitHub Releases, one tag per version (`theme-x.y.z`, `core-x.y.z`).

Published from the private ShopOS workspace via `tools/publish.sh`.
